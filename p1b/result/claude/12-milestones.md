# D3 — Milestones: Before / Now / Future

**Prompt:** P09 · **Run date:** 2026-09-13 · **Model:** Claude Opus 5 (1M context), `claude-opus-5[1m]`

Every NOW milestone carries an hours estimate and a REALISTIC / STRETCH / FANTASY verdict judged against 160 person-hours *minus* report, poster and demo. The budget verdict is the first line of the answer, by instruction.

---

**BUDGET VERDICT (first line, as requested): as re-costed below, the NOW plan totals 100.75 h against the 100 h that remains after 60 h of report/poster/demo/integration overhead — it EXCEEDS the budget by 0.75 h. Dropping milestone N5b (share-route hardening, 8.75 h) brings the plan to 92.00 h and restores an 8.00 h margin. Whether N5b is dropped is an OPEN TEAM DECISION, not a settled one, and naming that fork is the single most useful thing in this document.**

> **⚠ Corrected 2026-09-13 — three corrections to the sentence above, left visible on purpose.**
>
> **(i) False attribution to our own pre-mortem, withdrawn.** This line previously read *“our own pre-mortem says a 5% margin is the failure mode, not the plan.”* It does not. [`07-premortem.md`](07-premortem.md) contains no statement about margin, slack, buffer or reserve; grepping it for those words returns nothing. What it *does* say (line 16) is that **160 person-hours were spent and 29 of them (18%) touched the feature on the poster**, the other 131 going to environment setup, merge repair, demo-data seeding, a dead-end LLM experiment, and a report written in the last 48 hours. That is an **allocation** failure, not a **headroom** failure, and citing it for a margin claim was a misuse of our own document. **The judgement that a sub-5% margin is too thin for this team is mine, stated as mine**, and the only reasoning behind it is this: the pre-mortem's five-sixths overrun came from categories that *no line of the milestone table below enumerates*, so a margin of a few hours cannot absorb the one failure mode we have actually written down. **We have no measured overrun rate for this team — unknown.** What would settle it: the week-1 and week-2 burn-down readings that [`14-cut-list.md`](14-cut-list.md) already schedules. Until then the margin size is a judgement call, and it is labelled as one.
>
> **(ii) Arithmetic recomputed, not adjusted.** 12.0 + 24.0 + 22.0 + 24.0 + 18.75 = **100.75 h** against 160 − 60 = **100 h**, so the margin is **−0.75 h**, not +5.25 h. Removing N5b: 100.75 − 8.75 = **92.00 h**, margin **+8.00 h** — not the 14 h this line used to promise, which was computed against the pre-re-cost 94.75 total. [`14-cut-list.md`](14-cut-list.md) CUT 1 still prints “86.00 h / margin 14.00”; that pair also predates the N4 re-cost and is **superseded by 92.00 / 8.00**. Fix the cut list, not this file.
>
> **(iii) What the cut costs, said here and not only in the cut list.** N5b is the one defect this document proves has **no mitigation at any layer** — `shopping_list_shares` has no table, no RLS and no policy in 13 migrations (B4, re-verified today). Cutting it means the poster **must stop claiming the share route was closed**. The honest replacement sentence, taken from [`14-cut-list.md`](14-cut-list.md) CUT 1 and repeated here so this file can answer for itself: *“We found it, proved it, re-confirmed it on 2026-09-13, and priced it at 8.75 h; we did not fix it, because it is our first Supabase migration, our first RLS policy and an auth refactor, on a route the receipt does not touch.”* A finding reported found-and-not-fixed is a P1a deliverable; a finding reported fixed when it is not is a fabrication.
>
> **(iv) The fork this document must not close.** This file's re-cost note (below) calls dropping N5b *required*, and [`14-cut-list.md`](14-cut-list.md) records CUT 1 as **already fired**. But [`10-team-fit.md`](10-team-fit.md) §4 still schedules exactly this work as the 19–20 September skill-acquisition weekend, and its §6 budget still carries the share-route hole at 8.75 h as one of three **in-scope** defects. Both cannot be the plan. **Scope of the share route is one of the four decisions the team owns and this document deliberately does not settle.** The trigger and owner for the call are likewise **unknown** — no file names a person; naming one at the first cut checkpoint (**Sun** 2026-09-20, 18:00, per the cut list — *corrected 2026-09-13: 20 Sep 2026 is a Sunday*) is what would settle it.

Method note: every repository fact below was re-grepped against the working tree today (2026-09-13) by me — file paths, line numbers and row counts are first-hand. No market page was re-fetched this session (WebSearch budget was already exhausted in the adjudication run); every market claim is carried forward from the adjudication with its attribution intact and is marked as such. Where I could not verify, I write **unknown**.

Two corrections to the brief, both first-hand, both scope-relevant, are flagged inline at N4 and N2.

---

## BEFORE — Project 1a (our own run logs only)

**B1. We wrote and executed a functional test suite against an inherited codebase.**
Web: 32 tests, 31 PASS / 1 FAIL. Backend: 18 cases, 18 PASS. These are the tests *we* authored. The inherited suite's 1,095/1,096 in 3.6 s is the upstream team's result and appears nowhere on our poster.

**B2. We designed and ran an adversarial/security pass.**
Web: 44 cases, 33 PASS / 11 FAIL. Backend: 25 cases, 18 PASS / 7 FAIL. 18 recorded FAILs.
> *Corrected 2026-09-13 — the caveat belongs here, not three paragraphs down.* This line previously said “44 executed / 25 executed” with no qualification, which is where a marker reads it first. **Most of these cases assert on source text** (`expect(route).toContain(...)`), not on executed requests — the same retraction B4(a) makes below. **How many of the 69 are genuinely executed attacks is unknown**: no one has classified the cases one by one. That classification is one pass over `security-attack-cases.test.ts` and `test_adversarial_inputs.py` and is what would settle it. Until it is done, the poster may claim “69 adversarial cases written and run”, and may **not** claim “69 executed attacks”.

**B3. We triaged the failures to distinct source defects and costed them.**
18 FAILs collapse to 17 distinct defects; all 17 re-confirmed present in the working tree on 2026-09-13; the audit found an 18th. Competent fix + regression-test estimate: 24.25 h. The 18th is the worst of them: `backend/api/inventory_recommender.py` is 245 lines and `grep -n "validate\|verify\|check"` returns zero hits — *corrected 2026-09-13: that grep is **case-sensitive**, and a case-insensitive one matches the comment `# Validate and return` at :238. The defensible claim is narrower and still holds: no line of code recomputes or checks `match_score`*, while `RecipeRecommendationModal.tsx` renders the model's unchecked claims directly — `{recipe.reason}` at :298, the score at :303-307, and the expiring-ingredient names at :328-330 (all four confirmed by me today).

**B4. We corrected our own findings rather than defending them — the methodological deliverable.**
Three reversals, all re-verified today: (a) most P1a "security tests" are source-text greps, not executed attacks, and must be reported as such; (b) the headline IDOR is probably a system-level false positive — `shopping_list_items` has no `user_id` column and RLS enforces ownership through the parent list, so the fix the failing test demands would query a column that does not exist; (c) the real UC20 defect is better than the claimed one — `web/src/app/api/inventory/transfer/route.ts` never inspects affected row counts (:98 checks only `checkError`; the undo path at `:152-155` discards its result — *corrected 2026-09-13: that path is an `.update({ is_checked: false })`, **not** a DELETE; the file's only `.delete()` is `:170`, against `user_inventory`*), so it returns `success: true` with a `transferred_count` for transfers that did not happen, after the inventory row was already inserted. Silent data loss. Separately confirmed today: `web/src/app/api/shopping-lists/share/route.ts` builds a bare anon client, never calls `auth.getUser`, inserts on a body-supplied `shoppingListId`, and does `expiryDate.setDate(expiryDate.getDate() + expiryDays)` unbounded — and `shopping_list_shares` appears in **no** file under `supabase/migrations/` (13 migrations, listed today): no table, no RLS, no policy.

**B5. We surveyed the market first-party and ran a pre-mortem on ourselves.**
14 live products verified by direct fetch on 2026-09-13 (adjudication record, not re-fetched by me today), including one — "Use It Up: Pantry Recipes", App Store id 6775112024, v1.0.8 — that wounds our differentiation claim and is reported as wounding it. The pre-mortem named the week-1 decision that causes failure (treating the deliverable as a panel, not a number) and its top prevention by failure-prevented-per-hour: freeze the claims before the code.

*Honest gap to state on the poster: individual team biographies were not collected. What the repository proves is what we have **demonstrated** (tests, adversarial design, evidence discipline, technical writing), not what any of us **owns** as a skill. We have not yet shipped a production change to this Next.js app, written a Supabase migration, or changed the FastAPI recommender. The TypeScript/FastAPI fluency visible in this repository is the upstream team's (467 commits, ending 2025-12-07).*

---

## NOW — Project 2 (this month)

Direction: **THE RECEIPT.** Today the app prints a match percentage in 2xl bold at `RecipeRecommendationModal.tsx:304` that comes from `RecommendedRecipe.match_score` (`inventory_recommender.py:51` — confirmed today, the field is model-authored) and no line of our code recomputes it. The "Scoring Guidelines" at `inventory_recommender.py:159-162` are English sentences addressed to a language model. We move them into arithmetic and publish the terms.

| # | NOW milestone | Hours | Basis for the number *(column added 2026-09-13)* |
|---|---|---|---|
| N1 | Claim freeze, one-command environment, harness skeleton | 12.0 | **Unevidenced allocation.** No derivation exists in any file. |
| N2 | Deterministic scorer as a pure module, property-tested | 24.0 | **Partly evidenced.** The *work* is evidenced — ported prose at `inventory_recommender.py:159-162` plus the existing tested `recipeMatch.ts`. The *24.0* is an allocation. |
| N3 | The receipt on screen + authenticated server-side ranking | 22.0 | **Unevidenced allocation**, including the “6 of the 22 h” sub-split asserted in the classification table. |
| N4 | M0 harness, recorded baseline, correctability measure | 18.0 → **24.0** ⚠ | **Evidenced.** [`13-name-the-test.md`](13-name-the-test.md) builds F0+T1+T2+T5 line by line and reaches 24 h. The original 18.0 was an unevidenced allocation. |
| N5a | Quantity floor, restock date merge, affected-rows check | 10.0 | **3.5 h evidenced, 6.5 h unexplained.** Triage prices D6 (quantity) at 1.5 h and D4 (affected rows) at 2.0 h; the restock date-merge change is un-costed anywhere. See N5a below. |
| N5b | Share-route hardening | 8.75 | **Fully evidenced — the best-sourced number in this file.** Per-defect triage in [`03-engineering-evidence.md`](03-engineering-evidence.md): D1 2.5 + D2 1.75 + D3 1.0 + D11 3.5 = 8.75. |
| | **Total** | **94.75** → **100.75** ⚠ | |

> *Corrected 2026-09-13:* the 60 h of fixed overhead below (**report 24 / poster 12 / demo 10 / integration 14**) has no published derivation either and should be read as an **unevidenced allocation**, not a measurement. It is the largest single block in the budget and the least examined; a cut aimed at unevidenced numbers has 60 h to aim at before it reaches N5b's 8.75.

> ⚠ **Re-costed 2026-09-13.** [`13-name-the-test.md`](13-name-the-test.md) builds the M0 tests line by line and reaches **24 h**, not 18 — so N4 is under-costed by 6 h and the plan lands at **100.75 h against 100 h of capacity: the margin is gone and slightly negative.** This does not change the conclusion, it sharpens it: dropping **N5b** (share-route hardening, 8.75 h) is no longer the prudent option, it is the required one, restoring a ~8 h margin. The original 94.75 figure is left visible above so the correction is legible.

Against 160 h − 60 h fixed overhead (report 24, poster 12, demo 10, integration/merge/review/meeting 14) = **100 h.** ~~Margin: 5.25 h.~~ *Corrected 2026-09-13, recomputed from the line items:* **margin −0.75 h with N5b in (100.75 vs 100), +8.00 h with N5b out (92.00 vs 100).** The struck figure was computed against the pre-re-cost 94.75 total and is left visible so the correction is legible.

**N1 — Claim freeze, reproducible environment, harness skeleton (12 h).**
Write the Results paragraph on day 2 with numeric blanks, and beside each blank the exact query or harness invocation that fills it; delete any sentence whose query does not exist. Commit it. In the same milestone, land one committed setup path (`make dev` / devcontainer + a seeded local Supabase) so environment setup happens once in public instead of four times in private — P1a already diagnosed that defect and the pre-mortem shows it eating the month. Includes the empty harness that N2/N4 fill.

**N2 — Deterministic scorer, pure module, property-tested (24 h).**
Port `inventory_recommender.py:159-162` from prompt prose into a pure function over `(recipe_ingredients, inventory_rows, today)` returning `{total, line_items[]}` where each line item is `(inventory row id, ingredient name, expiry date, points, rule)`. Coverage half reuses `web/src/utils/inventory/recipeMatch.ts` (I read it today: pure, dependency-free, set membership on `ingredient_id`). Expiry half is a published integer table, Grocy's due score cited in a code comment, with its "20 points per expired ingredient" term **deliberately set to zero** — a food-safety position we defend, not an oversight. *Added 2026-09-13, because the position was asserted and never stated:* **Grocy's term rewards a recipe for consuming expired items; we refuse to let a score make a food-safety call at all, so the term is set to 0 rather than to some smaller positive number.** The override is an explicit field in the fixture (`p2/fixtures/grocy-due-score.json`) and flipping it 0→20 is a test — [`13-name-the-test.md`](13-name-the-test.md) T6 asserts each of the 500 totals moves by exactly 20 × that receipt's expired line-item count. The underlying reasoning is in [`10-team-fit.md`](10-team-fit.md) §7 and rests on FDA date-label guidance, which supports neither "expired, unsafe" nor "expired, discard".
> **Two open team decisions run straight through this milestone. This document names them and settles neither.**
> - **(b) What language is the scorer written in?** This milestone says "port `inventory_recommender.py:159-162` into a pure function" and reuses the TypeScript `recipeMatch.ts`; [`13-name-the-test.md`](13-name-the-test.md) runs T6/T9/T10/T11 under **Jest**; [`10-team-fit.md`](10-team-fit.md) §2 says "a deterministic **Python** ranker", §6 budgets "Deterministic ranker + **Pytest**" at 30 h, and its R2 role owns it in `backend/api/`. **Python and TypeScript are both written down as the answer. Unresolved.** It changes which half of the 24 h is a port and which is a rewrite, so it is a precondition for trusting N2's number, not a detail.
> - **(d) Zero-weight or excluded?** This milestone sets the expired term to **zero inside the score**; [`10-team-fit.md`](10-team-fit.md) §7 says **exclude past-date items from scoring** and surface them in a separate list; P1 in the FUTURE section below says replace the prose rule with an **enforced filter**. Scoring-at-zero and excluding-in-code are different systems with different tests (a zero-weighted expired item can still be *named on the receipt*; an excluded one cannot). **Unresolved — the team must pick one before T6's fixture is frozen.**

Property tests: every name in a line item resolves to a row in the input; total equals the sum of line items; permuting input order does not change output.
*Scoping correction, first-hand:* the corpus is real and exhaustively enumerable — `backend/dataset/recipes-supabase.csv` parses to exactly **50 rows**, `ingredients-supabase.csv` to **448**, and `recipe_ingredient_map-supabase.csv` exists, so the 50×10 = 500 offline cell count in the mission statement is sound.

**N3 — The receipt on screen, and the ranking moved server-side (22 h).**
Render the line items beneath the score in `RecipeRecommendationModal.tsx` (the UI contract already exists at :298, :303-307, :328-330), each line correctable in one tap, with Gemini demoted from judge to the prose describing a ranking it was handed. Includes closing the unauthenticated client→FastAPI call at `web/src/app/dashboard/inventory/page.tsx:141` (confirmed today: a bare `fetch` to `${BACKEND_URL}/inventory-recommend` with no auth header), which today spends our Gemini quota for anyone who knows the URL.

**N4 — M0 harness, recorded baseline, correctability measure (~~18 h~~ → 24 h, re-costed 2026-09-13 against [`13-name-the-test.md`](13-name-the-test.md)).**
Offline: across 50 recipes × 10 seeded pantries = 500 recommendations, displayed total equals the sum of displayed line items in 500/500, and ordering is byte-identical across 20 shuffles of inventory input order. Baseline: the same harness against today's build, responses snapshotted to JSON so the baseline is auditable and never needs re-running. Correctability: after one flagged line is corrected, the recorded fraction of the 500 rankings whose order changes — the non-tautological half, which no amount of careful coding can guarantee in advance.
*Scoping correction, first-hand, and it changes a number on the poster:* the claim "a baseline of 0/500 recorded by the same harness against today's build" **cannot be produced as written.** `InventoryRecommendRequest.num_recipes` is `Field(default=5, ge=1, le=10)` — today's build returns at most 10 recommendations per pantry, so 10 seeded pantries yield at most 100, not 500. State the baseline in its reproducible form: *0 of **300** displayed scores decompose into line items — 30 live calls over 100 distinct (pantry, rank-position) cells, 3 repeats — and across those repeats the returned recipe sets and scores disagree at rate X* — with X measured, not asserted. That costs **30** live Gemini calls, once, archived. *(Corrected 2026-09-13: "100-300 calls" confused calls with returned objects — 10 pantries × 3 repeats is 30 calls, each returning up to 10 recommendations.)*

**N5 — Security and data-integrity floor (18.75 h).**
*N5a (10 h — see the re-cost note below; only 3.5 h of this is evidenced), on the receipt's critical path:* reject non-positive and non-finite quantities at the write (`quantity || 1` at `transfer/route.ts:65, :80, :167` against `user_inventory.quantity DECIMAL(10,2) NOT NULL DEFAULT 1` with no CHECK — negatives persist and 0 silently becomes 1, so the receipt would print points for food that isn't there); change the restock merge at `transfer/route.ts:66` from `item.expiration_date || existingItem.expiration_date` to the earlier of the two (confirmed today — the incoming date currently overwrites the stored one while quantities are summed, so fresh spinach on old spinach makes the whole aggregate row look fresh, and a receipt printing that date would overstate freshness); check affected row counts in the transfer route so `success: true` stops being returned for writes that did not happen.
> **⚠ N5a is priced above its own evidence — flagged 2026-09-13, not silently re-costed.** The team's per-defect triage in [`03-engineering-evidence.md`](03-engineering-evidence.md) prices two of these three edits directly: **D6 (`quantity || 1`) = 1.5 h** and **D4 (unchecked affected rows) = 2.0 h**, fix plus regression test, **3.5 h together**. [`10-team-fit.md`](10-team-fit.md) §6 uses exactly those two figures. The third edit — the restock date merge at `transfer/route.ts:66` — is a finding of this audit and **is costed nowhere**, so **6.5 h of the 10.0 has no stated basis**. That unexplained block is larger than the whole margin the budget verdict turns on, and larger than N5b, the milestone the plan proposes to cut. **We are not re-costing it to a number we cannot defend**: what the date-merge edit costs is **unknown** until someone prices it the way D4 and D6 were priced — one pass over `transfer/route.ts:66` plus the merge-direction cases [`13-name-the-test.md`](13-name-the-test.md) T10 already enumerates (8 cases). That estimate, not a guess, is what closes the 6.5 h. Until it exists, treat N5a as **3.5 h evidenced + 6.5 h unevidenced**, and aim any cut at unevidenced hours before evidenced ones.

*N5b (8.75 h), not on the critical path:* `share/route.ts` — write the missing migration for `shopping_list_shares`, `ENABLE ROW LEVEL SECURITY` plus a policy, then `auth.getUser`, then the ownership query on `shoppingListId`, then bound `expiryDays`.

---

## Classification of every NOW milestone

Judged against 160 person-hours minus 60 h of report/poster/demo/integration — four graduate students at ~10 h/week, not a funded team. *Corrected 2026-09-13:* **the ~10 h/person/week is assumed, not declared.** [`10-team-fit.md`](10-team-fit.md) §5 flags this exact number as an assumption — *"If one person has four, the plan changes. Say so out loud"* — and every REALISTIC/STRETCH verdict below inherits it as an unmeasured input. **A real availability declaration from all four members in week 1 is a precondition for these verdicts**, and no verdict below should be quoted without it.

| # | Verdict | Why, in one sentence |
|---|---|---|
| N1 | **REALISTIC** | It is writing and configuration, not research; it is the pre-mortem's highest-yield prevention, and skipping it is what turns the other four milestones into **the failure our pre-mortem imagined, in which only 29 of 160 hours reach the poster's feature** — *corrected 2026-09-13: that 29/160 is an invented figure from a deliberately fictional autopsy dated forward to 2026-10-13 and written in the past tense on purpose, not a forecast and not a measurement.* |
| N2 | **REALISTIC** | The scoring rules already exist as English at `inventory_recommender.py:159-162` and the coverage half already exists as tested, dependency-free code in `recipeMatch.ts`, so this is a port plus an integer table, and it is the only milestone whose difficulty we can bound in advance. |
| N3 | **REALISTIC** | The UI contract, the data assembly and the render sites already exist; the genuine unknown is the one-tap correction write path, which is why 6 of the 22 h are allocated to it and why N4's correctability measure is the thing that would expose a shortcut. |
| N4 | **STRETCH** | The offline half runs in seconds over a corpus we can enumerate exhaustively and is REALISTIC on its own; the recorded live baseline depends on ~100-300 Gemini calls against a non-deterministic endpoint, which is the part that slips. *Largest realistic slice if it slips:* publish the offline 500/500 and 20-shuffle results in full, and report the baseline as a single archived 10-pantry × 3-run snapshot with the disagreement rate computed from it — one run, recorded once, never re-run. |
| N5 | **STRETCH as a whole; N5a alone is REALISTIC** | N5a is three bounded edits to one file that the receipt's honesty actually depends on, though only 3.5 of its 10.0 h are evidenced (see N5a above); N5b is our first-ever Supabase migration plus an RLS policy plus an auth refactor, attempted by a team that has never written a migration in this repository ~~, and its 8.75 h estimate is the least evidenced number in this document~~ — ***Corrected 2026-09-13: this was exactly backwards.*** 8.75 is the **only** milestone number in this file with a published derivation (D1 2.5 + D2 1.75 + D3 1.0 + D11 3.5, from the per-defect triage); N1's 12.0, N3's 22.0, N5a's 6.5 h remainder and the 24/12/10/14 overhead split have none. The document performed humility about its best-sourced number and aimed the cut at it while leaving the unsourced ones untouched. *Largest realistic slice of N5b, restated:* write the migration and the RLS policy only, and read what it actually does — **it is not "no defence exists" → "a defence exists"**, because there is no table for the route to write into at all, so the slice **creates the write target this route has been failing into** and its net effect depends entirely on the policy being right against a route that holds an **anon** client (`share/route.ts:9-12`), for which no `auth.uid()` policy can ever match. On that reading the slice is a **new surface**, not a defence, unless its deliverable is the **executed** test that an anon insert into `shopping_list_shares` is refused — make that test the acceptance criterion, not the migration file. **The ~4 h and the 4.75 h returned to the margin are allocations with no basis and should not be quoted**; split the evidenced 8.75 only once someone prices the two halves. Report the remaining auth/ownership/bounds work as known-open with the file and line named. |

**No NOW milestone is FANTASY, by construction — we removed them before writing the list.** The three the team will be tempted to re-add mid-month, and the largest realistic slice of each:

| Tempting addition | Verdict | Largest realistic slice |
|---|---|---|
| Uncertainty-aware ranking ("probability the item is still present") | **FANTASY** | The schema forbids it: `20251129030000_user_inventory.sql` has `UNIQUE (user_id, ingredient_id, location)` and sums quantities into one row, so there are no lots, no purchase dates and no consumption log to condition on. Realistic slice: ship one honest flag — "this item's quantity was last confirmed N days ago" — computed from `updated_at`, with no probability claimed. And say **"item"**, never "lot". |
| A user study showing the receipt changes what people cook | **FANTASY** | The nearest published trial (JMIR PMC9482070) ran 6 students for a month and found no change; that is our entire budget spent on measurement alone, with IRB on top. Realistic slice: 4 scripted walkthroughs with teammates on the seeded pantries, reported as usability observations with the word "study" nowhere near them. |
| A verification gate that checks every sentence the model writes (Candidate 2's `verifyExplanation`) | **FANTASY this month** | It needs a mutation corpus, an injector, and a suppression UI on top of everything in N2-N4, and it competes directly with the receipt for the same hours. Realistic slice: since N2 already resolves every line item to an input row, hard-suppress the `expiring_ingredients_used` render at `RecipeRecommendationModal.tsx:328-330` whenever a name does not resolve — roughly 2 h, folded into N3, delivering the safety property without the apparatus. |

---

## FUTURE — Project 3

**P1. Move food safety from prose into code.** `inventory_recommender.py:157` reads `5. **NEVER**: Do not recommend recipes that ONLY use ❌ EXPIRED items` — a sentence addressed to a model, not a code path, while expired rows are injected at :89 as `(EXPIRED N days ago) ❌`. Replace it with an enforced filter, and resolve the open policy question of what a date label on screen commits us to (regulator guidance: **unknown**, not retrieved).

**P2. Per-purchase lots.** Add the table the current schema forbids — per-lot quantity, purchase date, expiry, provenance — replacing the summing UNIQUE constraint. This is the precondition for every remaining item on this list, and the precondition for ever writing the word "lot" in our own copy.

**P3. Uncertainty-aware ranking on top of lots plus a correction log. HYPOTHESIS, not an established need.** The emptiest cell in our market survey (differentiation 3, fit 1 today). *Corrected 2026-09-13 — the evidence was overstated:* this previously read "the vendor evidence that the problem is real is Plan to Eat's own post-mortem". **A vendor's own sentence is evidence that a vendor said it, not that the problem is real.** What we actually have is: **one vendor asserts the sync problem is unsolvable in its own product** — "There is no way for your real inventory and your Plan to Eat inventory to ever remain synchronized" (adjudication record, verified 2026-09-13, not re-fetched by me) — and **we have no demand-side evidence at all that users want an explanation for it**: our own gap-closing sweep of **851 unique Reddit entries plus ~500 app-store reviews across eleven explainability phrasings returned zero hits** ([`17-gap-closing.md`](17-gap-closing.md)). Whether that absence means latent demand or no demand is **unknown**, and only user research settles it — which is a Project 2 M0 task, not desk research. This is the same shape as the App Store screenshot that turned out to be a Sketch mockup, and P3 is labelled a hypothesis for that reason.

**P4. The verification gate, fully built.** Every ingredient an explanation asserts, checked against the 448-row catalog and the caller's own `user_inventory` read under RLS, with suppression made visible rather than hidden — with the mutation corpus and injector that make the ≥49/50 detection claim testable.

**P5. The measurement Project 2 cannot afford.** A within-subject evaluation of whether a checkable receipt changes cooking choices, powered against the null result the nearest published trial actually found.

**Open question we should carry forward, unresolved and labelled:** whether "Use It Up" (App Store id 6775112024) computes its per-card "why this dish" from pantry rows and dates or only from a WHOOP recovery signal. Its v1.0.5 note ties the explanation to recovery, its iTunes payload carries no `sellerUrl`, and no first-party text settles it. **Unknown.**

> ***Corrected 2026-09-13 — the surviving claim as written is already false, and is replaced.*** The old wording ("no product shows a rationale *computed from, and checkable against, the specific inventory rows and dates that produced the ranking*") was said to be "true either way". It is not: **Remy** prints *"Using up before they expire: &lt;ingredient&gt; · &lt;qty&gt;"* directly above its suggested recipes, computed from inventory rows and verified from its live web bundle's i18n table and render function, and **Cooklist** ships item-naming expiry reminders. The claim that survives later runs is a **four-way conjunction**, and it must be quoted as the conjunction, never as one leg:
>
> > No shipped consumer meal product joins all four facts in one auditable, correctable explanation: the **named pantry item**, its **actual expiry date**, the **nutrient constraint** the dish satisfies, and the **reason for the substitution** it made.
>
> Each leg is individually shipped, which is the point: **Cooklist** shows what it swapped but never why, and never argues from a nutrient target. **RecipeFix** argues about the swap but holds no inventory and no dates. **Remy** has the inventory and the dates but states neither the date nor a reason. **Grocy** has the formula but shows the user a bare integer. *(The audit finding that prompted this correction named Mealie as the fourth holder; our own market survey names **Grocy**, and that is what is used here.)* And the caveat travels with the claim: **this is a conjunction gap, not a moat** — Remy already holds every input.
