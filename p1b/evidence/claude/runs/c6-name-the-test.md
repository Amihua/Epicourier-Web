# C6-name-the-test

**Workflow:** P1b Claude decide and commit (run 3)  
**Phase:** Commit  
**Model:** Claude Opus 5 (1M context), `claude-opus-5[1m]`  
**Agent id:** `a3aabbf3139a53dee`  
**Tool calls:** 17 total — 0 web searches, 0 pages fetched  
**Raw transcript:** `raw/agent-a3aabbf3139a53dee.jsonl` (full tool-call trace, retained)

---

## Prompt, exactly as issued

```text
For each claimed benefit below, write the TEST that would prove it — not a description of a
test, the test itself. This is prompt C6, of our own invention.

THE MISSION CANDIDATES AND THEIR "SO WHAT" CLAIMS:
CANDIDATE 1 — THE RECEIPT

All fourteen live products our survey verified on 2026-09-13 tell you that a recipe matches and none tells you why; the strongest thing anyone ships is Samsung Food's "each recipe will show how well it matches", which is a number, not a reason. Ours is worse than theirs, because the percentage we print in 2xl bold at RecipeRecommendationModal.tsx:304 comes from RecommendedRecipe.match_score at backend/api/inventory_recommender.py:51 — a field Gemini writes and no line of our code recomputes from the dates we already hold. Epicourier will compute that score in a pure module over the fifty-recipe corpus in backend/dataset/recipes-supabase.csv, using a published integer table — Grocy's due score, cited in a code comment, with its "20 points per expired ingredient" term deliberately set to zero — and print the receipt beneath it: each pantry item, its expiry date, its points, the running total, every line correctable in one tap, with Gemini demoted from judge to the prose that describes a ranking it was handed. The measurable claim M0 will test offline: across 50 recipes × 10 seeded pantries = 500 recommendations, the displayed total equals the sum of the displayed line items in 500/500 cases and the ordering is byte-identical across 20 shuffles of inventory input order, against a baseline of 0/500 recorded by the same harness against today's build. A user who believes the recommendation is wrong can then point at the one line that is wrong and watch the ranking move, instead of arguing with a percentage nobody — including us — can reproduce.

CANDIDATE 2 — THE VERIFIED SENTENCE

Recommenders in this market talk, and nothing checks that what they say is true: the decisive column of our survey is "unknown" or "partial" for 14/14 confirmed rivals, and SuperCook's apparent explanation was corrected from "yes" to "unknown" by our own fact-checker because a filter is not an explanation. Our text is not even a model's opinion — recommender.py:210-213 emits the template f"Selected because it aligns with goal '{goal_text}' and differs from other meals.", the user's own free-text goal echoed back, while UC17's prose is generated from a prompt that the user's ingredient names are interpolated into with no untrusted-data boundary. Epicourier will put a gate between the model and the screen: verifyExplanation checks every ingredient the explanation asserts against the 448-row catalog in backend/dataset/ingredients-supabase.csv and the caller's own user_inventory read under RLS, suppresses any sentence naming food the user does not own, and marks the suppression visibly rather than hiding it — and the ranking moves server-side behind an authenticated route, closing the client-to-FastAPI call at dashboard/inventory/page.tsx:141 that today spends our Gemini quota for anyone who knows the URL. The measurable claim M0 will test offline: on a fixed corpus of 100 stored explanations, 50 of them mutated by an automated injector that swaps in an ingredient the seeded pantry does not contain, the gate flags ≥49/50 mutated cases and falsely flags ≤1/50 clean ones, against a baseline of 0/50 detection on today's build, where no checking code path exists. Everything Epicourier says about your food is then either checkable against a row you can see, or it is not said.

CANDIDATE 3 — THE HONEST PANTRY

Plan to Eat built a pantry inventory, removed it, and published why — "There is no way for your real inventory and your Plan to Eat inventory to ever remain synchronized" — so the question that decides whether a pantry feature is worth shipping is not whether it exists but whether it admits what it does not know. Ours does not admit anything: calculateRecipeMatch at web/src/utils/inventory/recipeMatch.ts:29 does set membership on ingredient_id and ignores quantity and expiry entirely, so a row sitting at quantity 0 — or negative, courtesy of quantity || 1 at api/inventory/transfer/route.ts:65 against a DECIMAL column with no CHECK constraint — or three weeks past its date still counts as fully available, and prompt rule 5 at inventory_recommender.py:157 forbids only recipes made *entirely* of expired items. Epicourier will make availability a dated, quantity-aware judgement: non-positive and non-finite quantities are rejected at the write, items past their expiration_date contribute zero and are shown with the date that disqualified them, and before a seven-meal plan is committed the app asks one targeted question about the single item whose true state would most change the ordering. The measurable claim M0 will test offline: on 50 recipes × 10 seeded pantries containing planted rows at quantity 0, quantity −5, and expiration_date in the past, 0/500 recommendations report any such item as available, against a baseline of 500/500 recorded by the same harness on today's build, where every planted row is counted as present by construction. The pantry stops being a claim about your kitchen and becomes a dated record you can correct, which is the only version of this feature a vendor who has already retreated from it did not try.

---

PICK: CANDIDATE 1 — THE RECEIPT

Why. It is the only one of the three aimed at the single gap that survived adversarial interrogation (G1; G2 and G4 are dead, G3 is narrowed to a claim about propagating uncertainty that Candidate 3 only half delivers). It is buildable inside the ~100 h of feature-and-test left after the 60 h of fixed overhead, because its core asset already exists and is pure, dependency-free and unit-tested: recipeMatch.ts supplies the coverage half, and the expiry half is an integer table. Its claim is falsifiable by a harness that runs in seconds over a 50-row corpus we can enumerate exhaustively, with no users, no field study, and no dependence on the two things our own verification could not reach this session (Samsung Food behind HTTP 403, and regulator date-label guidance). And it forces the honest version of our own position into the product rather than into a footnote — we ship the market's weakness today, in a worse form, and the fix is visible on screen.

The sentence a marker challenges first. Sentence 4: "the displayed total equals the sum of the displayed line items in 500/500 cases and the ordering is byte-identical across 20 shuffles of inventory input order, against a baseline of 0/500." The challenge is that this is a tautology dressed as a result — if you write the code so the total is the sum, of course the test passes; you have measured that addition works, and a 0/500 baseline is guaranteed because the current build has no line items to sum.

How we defend it. First, restate what is under test: not that addition works, but that the number shown to the user is the number the system actually ranked with, and that it is stable under nuisance variation in the input. Today's build fails both properties for reasons that are structural, not accidental — inventory_recommender.py:51 has the model author the integer, so there is nothing to sum and no reason for two runs over the same pantry to agree — and we report the baseline as the same harness executed against the current build on the same 500 cases, including the observed run-to-run disagreement rate of today's match_score, so the 0/500 is a measurement and not a definition. Second, we concede the formula is not the contribution and say so on the poster: Grocy already publishes an auditable due score, so our contribution is per-recommendation publication of the terms plus one named, defended disagreement — Grocy's 20 points per expired ingredient against our 0, which is a food-safety position we take deliberately given that rule 5 at inventory_recommender.py:157 currently lets expired items ride along. Third, we add the non-tautological half of the same harness: the correctability measure — after a single flagged line is corrected, the recorded fraction of the 500 rankings whose order changes, which no amount of careful coding can guarantee in advance and which tests whether the receipt is connected to anything. If the marker still calls the invariant trivial, the correct answer is that it is trivial and we do not have it, which is precisely the finding the survey produced about fourteen competitors as well.

THE MILESTONES:
**BUDGET VERDICT (first line, as requested): the NOW plan totals 94.75 h against the ~100 h that remains after 60 h of report/poster/demo/integration overhead — it does NOT exceed the budget, but it clears it by only 5.25 h (5%), and our own pre-mortem says a 5% margin is the failure mode, not the plan. Drop milestone N5b (share-route hardening, 8.75 h) to restore a 14 h margin; that is the single most useful sentence in this document.**

Method note: every repository fact below was re-grepped against the working tree today (2026-09-13) by me — file paths, line numbers and row counts are first-hand. No market page was re-fetched this session (WebSearch budget was already exhausted in the adjudication run); every market claim is carried forward from the adjudication with its attribution intact and is marked as such. Where I could not verify, I write **unknown**.

Two corrections to the brief, both first-hand, both scope-relevant, are flagged inline at N4 and N2.

---

## BEFORE — Project 1a (our own run logs only)

**B1. We wrote and executed a functional test suite against an inherited codebase.**
Web: 32 tests, 31 PASS / 1 FAIL. Backend: 18 cases, 18 PASS. These are the tests *we* authored. The inherited suite's 1,095/1,096 in 3.6 s is the upstream team's result and appears nowhere on our poster.

**B2. We designed and executed an adversarial/security pass.**
Web: 44 executed, 33 PASS / 11 FAIL. Backend: 25 executed, 18 PASS / 7 FAIL. 18 recorded FAILs.

**B3. We triaged the failures to distinct source defects and costed them.**
18 FAILs collapse to 17 distinct defects; all 17 re-confirmed present in the working tree on 2026-09-13; the audit found an 18th. Competent fix + regression-test estimate: 24.25 h. The 18th is the worst of them: `backend/api/inventory_recommender.py` is 245 lines and `grep -n "validate\|verify\|check"` returns zero hits, while `RecipeRecommendationModal.tsx` renders the model's unchecked claims directly — `{recipe.reason}` at :298, the score at :303-307, and the expiring-ingredient names at :328-330 (all four confirmed by me today).

**B4. We corrected our own findings rather than defending them — the methodological deliverable.**
Three reversals, all re-verified today: (a) most P1a "security tests" are source-text greps, not executed attacks, and must be reported as such; (b) the headline IDOR is probably a system-level false positive — `shopping_list_items` has no `user_id` column and RLS enforces ownership through the parent list, so the fix the failing test demands would query a column that does not exist; (c) the real UC20 defect is better than the claimed one — `web/src/app/api/inventory/transfer/route.ts` never inspects affected row counts (:98 checks only `checkError`; the DELETE path discards its result), so it returns `success: true` with a `transferred_count` for transfers that did not happen, after the inventory row was already inserted. Silent data loss. Separately confirmed today: `web/src/app/api/shopping-lists/share/route.ts` builds a bare anon client, never calls `auth.getUser`, inserts on a body-supplied `shoppingListId`, and does `expiryDate.setDate(expiryDate.getDate() + expiryDays)` unbounded — and `shopping_list_shares` appears in **no** file under `supabase/migrations/` (13 migrations, listed today): no table, no RLS, no policy.

**B5. We surveyed the market first-party and ran a pre-mortem on ourselves.**
14 live products verified by direct fetch on 2026-09-13 (adjudication record, not re-fetched by me today), including one — "Use It Up: Pantry Recipes", App Store id 6775112024, v1.0.8 — that wounds our differentiation claim and is reported as wounding it. The pre-mortem named the week-1 decision that causes failure (treating the deliverable as a panel, not a number) and its top prevention by failure-prevented-per-hour: freeze the claims before the code.

*Honest gap to state on the poster: individual team biographies were not collected. What the repository proves is what we have **demonstrated** (tests, adversarial design, evidence discipline, technical writing), not what any of us **owns** as a skill. We have not yet shipped a production change to this Next.js app, written a Supabase migration, or changed the FastAPI recommender. The TypeScript/FastAPI fluency visible in this repository is the upstream team's (467 commits, ending 2025-12-07).*

---

## NOW — Project 2 (this month)

Direction: **THE RECEIPT.** Today the app prints a match percentage in 2xl bold at `RecipeRecommendationModal.tsx:304` that comes from `RecommendedRecipe.match_score` (`inventory_recommender.py:51` — confirmed today, the field is model-authored) and no line of our code recomputes it. The "Scoring Guidelines" at `inventory_recommender.py:159-162` are English sentences addressed to a language model. We move them into arithmetic and publish the terms.

| # | NOW milestone | Hours |
|---|---|---|
| N1 | Claim freeze, one-command environment, harness skeleton | 12.0 |
| N2 | Deterministic scorer as a pure module, property-tested | 24.0 |
| N3 | The receipt on screen + authenticated server-side ranking | 22.0 |
| N4 | M0 harness, recorded baseline, correctability measure | 18.0 |
| N5 | Security and data-integrity floor (N5a 10.0 + N5b 8.75) | 18.75 |
| | **Total** | **94.75** |

Against 160 h − 60 h fixed overhead (report 24, poster 12, demo 10, integration/merge/review/meeting 14) = **100 h. Margin: 5.25 h.**

**N1 — Claim freeze, reproducible environment, harness skeleton (12 h).**
Write the Results paragraph on day 2 with numeric blanks, and beside each blank the exact query or harness invocation that fills it; delete any sentence whose query does not exist. Commit it. In the same milestone, land one committed setup path (`make dev` / devcontainer + a seeded local Supabase) so environment setup happens once in public instead of four times in private — P1a already diagnosed that defect and the pre-mortem shows it eating the month. Includes the empty harness that N2/N4 fill.

**N2 — Deterministic scorer, pure module, property-tested (24 h).**
Port `inventory_recommender.py:159-162` from prompt prose into a pure function over `(recipe_ingredients, inventory_rows, today)` returning `{total, line_items[]}` where each line item is `(inventory row id, ingredient name, expiry date, points, rule)`. Coverage half reuses `web/src/utils/inventory/recipeMatch.ts` (I read it today: pure, dependency-free, set membership on `ingredient_id`). Expiry half is a published integer table, Grocy's due score cited in a code comment, with its "20 points per expired ingredient" term **deliberately set to zero** — a food-safety position we defend, not an oversight. Property tests: every name in a line item resolves to a row in the input; total equals the sum of line items; permuting input order does not change output.
*Scoping correction, first-hand:* the corpus is real and exhaustively enumerable — `backend/dataset/recipes-supabase.csv` parses to exactly **50 rows**, `ingredients-supabase.csv` to **448**, and `recipe_ingredient_map-supabase.csv` exists, so the 50×10 = 500 offline cell count in the mission statement is sound.

**N3 — The receipt on screen, and the ranking moved server-side (22 h).**
Render the line items beneath the score in `RecipeRecommendationModal.tsx` (the UI contract already exists at :298, :303-307, :328-330), each line correctable in one tap, with Gemini demoted from judge to the prose describing a ranking it was handed. Includes closing the unauthenticated client→FastAPI call at `web/src/app/dashboard/inventory/page.tsx:141` (confirmed today: a bare `fetch` to `${BACKEND_URL}/inventory-recommend` with no auth header), which today spends our Gemini quota for anyone who knows the URL.

**N4 — M0 harness, recorded baseline, correctability measure (18 h).**
Offline: across 50 recipes × 10 seeded pantries = 500 recommendations, displayed total equals the sum of displayed line items in 500/500, and ordering is byte-identical across 20 shuffles of inventory input order. Baseline: the same harness against today's build, responses snapshotted to JSON so the baseline is auditable and never needs re-running. Correctability: after one flagged line is corrected, the recorded fraction of the 500 rankings whose order changes — the non-tautological half, which no amount of careful coding can guarantee in advance.
*Scoping correction, first-hand, and it changes a number on the poster:* the claim "a baseline of 0/500 recorded by the same harness against today's build" **cannot be produced as written.** `InventoryRecommendRequest.num_recipes` is `Field(default=5, ge=1, le=10)` — today's build returns at most 10 recommendations per pantry, so 10 seeded pantries yield at most 100, not 500. State the baseline in its reproducible form: *0 of 100 displayed scores decompose into line items, and across 3 repeat runs of the same 10 pantries the returned recipe sets and scores disagree at rate X* — with X measured, not asserted. That costs roughly 100-300 live Gemini calls, once, archived.

**N5 — Security and data-integrity floor (18.75 h).**
*N5a (10 h), on the receipt's critical path:* reject non-positive and non-finite quantities at the write (`quantity || 1` at `transfer/route.ts:65, :80, :167` against `user_inventory.quantity DECIMAL(10,2) NOT NULL DEFAULT 1` with no CHECK — negatives persist and 0 silently becomes 1, so the receipt would print points for food that isn't there); change the restock merge at `transfer/route.ts:66` from `item.expiration_date || existingItem.expiration_date` to the earlier of the two (confirmed today — the incoming date currently overwrites the stored one while quantities are summed, so fresh spinach on old spinach makes the whole aggregate row look fresh, and a receipt printing that date would overstate freshness); check affected row counts in the transfer route so `success: true` stops being returned for writes that did not happen.
*N5b (8.75 h), not on the critical path:* 

OUR CODEBASE, so the fixtures are real: Next.js 15 app under web/ with Jest (config at
web/tests/p1a/jest.config.ts), FastAPI backend under backend/ with pytest (tests under
backend/tests/), Supabase/PostgreSQL with migrations under supabase/migrations/, and
backend/api/inventory_recommender.py holding build_prompt() and the Gemini call.
user_inventory upserts by (user_id, ingredient_id, location) and SUMS quantities — there are no
per-purchase lots.

Output a table:
| Benefit | Test name | Fixture (exact inputs and starting state) | Assertion | Threshold that
decides pass/fail | Baseline it is compared against | Who or what produces the number | Hours to
build the test |

RULES:
- A threshold is a NUMBER. "Improved" is not a threshold. "Most users" is not a threshold.
- A baseline is a measurement of the CURRENT state, taken BEFORE the change. If no baseline
  exists, the first row of your table is the work of measuring it.
- If a claimed benefit cannot be given a test that four students could run in one month, mark it
  UNTESTABLE and say so. We will DELETE it from the mission statement rather than ship a claim we
  cannot check. LIST THE UNTESTABLE ONES FIRST.
- Prefer tests that run offline with no human subjects, because those are the only ones that
  reliably finish.

After the table, write the one paragraph that goes in the report explaining how M0's evaluation
will be run and what would make it fail.
```

## Response

| # | Benefit (as claimed) | Test name | Fixture (exact inputs and starting state) | Assertion | Threshold that decides pass/fail | Baseline it is compared against | Who or what produces the number | Hours |
|---|---|---|---|---|---|---|---|---|
| **U1** | **UNTESTABLE — DELETE.** "A user who believes the recommendation is wrong can point at the one line that is wrong" (the comprehension/trust half) | none | would require ≥30 recruited users, two arms, a comprehension instrument | — | none possible: "can point at" has no offline operationalisation | — | human subjects; not available in one month | 0 |
| **U2** | **UNTESTABLE — DELETE the normative clause.** "Setting Grocy's 20-points-per-expired term to **zero** is the right food-safety position" | none | no ground truth exists; regulator date-label guidance was unreachable in the P1b run | — | none: this is a policy choice, not a measurement | — | would need a food-safety authority we could not fetch | 0 |
| **U3** | **UNTESTABLE as stated — downgrade to a dated citation.** "All fourteen live products tell you a recipe matches and none tells you why" | none | Samsung Food returned HTTP 403 in P1b; 14 vendors are a moving target and re-fetching is out of budget | — | none: a market census is not re-runnable offline | — | WebSearch/WebFetch budget already exhausted; keep the 2026-09-13 archived snapshots in `p1b/evidence/` and cite them with the date, do not re-assert as current | 0 |
| **U4** | **UNTESTABLE — DELETE.** "Gemini is demoted to prose that *describes* the ranking" (the **faithfulness** half: the sentence is true of the receipt) | none | this is Candidate 2's verifier, which we rejected; needs the 448-row catalog gate + a mutation corpus we did not budget | — | none | — | — (the *influence* half is testable and is row T7) | 0 |
| **U5** | **UNTESTABLE — never claim it.** "The receipt makes the recommendations *better*" | none | would need relevance labels for 50 recipes × 10 pantries | — | none: no ground-truth ranking exists | — | human raters | 0 |
| **F0** | **BASELINE ROW (do this first).** Today's displayed score is model-authored and non-reproducible; there is nothing to sum | `baseline_snapshot_live_gemini` | Freeze 10 pantries `web/tests/p2/fixtures/pantries/P01–P10.json` (6–30 rows each, drawn from the 448 catalog rows, clock pinned to 2026-10-01; each plants ≥2 items ≤3 d to expiry, ≥2 ≤7 d, ≥1 already expired, ≥1 NULL `expiration_date`). POST each to the running `@app.post("/inventory-recommend")` (`backend/api/index.py:64`) with `num_recipes=10` (the cap at `inventory_recommender.py:42`), **3 repeats** = 30 live `gemini-2.5-flash` calls (`:217`); archive all 30 raw JSON bodies | (a) count of returned `match_score` values that decompose into line items; (b) **X** = fraction of (pantry, rank-position) cells where `recipe_id` or `match_score` differs across the 3 repeats | No pass/fail — this is the measurement. It fails only if <30 responses are archived. (a) is 0/100 **by construction** (the schema at `:44–52` has no line-item field) and is reported as definitional; (b) **X is the real baseline number**, reported to 2 dp | none — it *is* the baseline | `p2/baseline/run_baseline.py` (pytest + httpx, already a backend dev dep); output committed as JSON so it never re-runs | 9 |
| **T1** | The displayed total is the sum of the displayed line items | `test_receipt_total_equals_sum_of_line_items` | 50 recipes × 10 pantries = **500** cells. Corpus = `recipes-supabase.csv` (50 rows), `recipe_ingredient_map-supabase.csv` (528 rows, 4–18 ingredients/recipe, mean 10.56), `ingredients-supabase.csv` (448 rows). Assert on the **serialized DTO the modal consumes**, not the function return | `total === line_items.reduce((a,l)=>a+l.points,0)` and `Number.isInteger(total)` and every `points` is an integer (no float path). Any display clamp/round must appear as its own line item or the test fails | **500/500** exact equality; zero tolerance | F0(a): 0/100 | Jest, `web/tests/p2/receipt-invariants.test.ts` (needs `testMatch` in `web/tests/p1a/jest.config.ts` widened to `tests/p2/**`) | 4 |
| **T2** | The ordering is stable under nuisance variation in input order | `test_ranking_byte_identical_under_input_permutation` | Each of the 10 pantries permuted **20** times by a committed permutation table (`p2/fixtures/permutations.json`, Fisher–Yates seeds 1–20, committed so the shuffles are auditable), scorer run over all 50 recipes each time = 200 full rankings | SHA-256 of the canonical-JSON ranked list (`recipe_id`, `total`, `line_items` in emission order) is identical across the 20 permutations of the same pantry. **Ties must break on `recipe_id` ascending** — V8's stable sort preserves *permuted* input order for ties, so this is the one place the test genuinely fails | **10/10** pantries yield exactly 1 distinct digest (200/200 digests equal within pantry); zero tolerance | F0(b) = **X**, measured under *identical* input — a strictly easier condition, so any X>0 is already a baseline failure | Jest | 4 |
| **T3** | The number on screen is the number the system ranked with (no model-authored score reaches the render path) | `test_no_model_authored_score_on_the_render_path` | (a) static: AST/grep over `web/src/components/inventory/**`; (b) dynamic: mock the backend client to return `match_score: 99` for every recipe **and** the recommendations in reverse rank order; render `RecipeRecommendationModal` with pantry P03 | (a) zero reads of the response's `match_score` on the render path (today: `RecipeRecommendationModal.tsx:303–304` and `:306–307`); (b) rendered order and rendered totals are byte-identical to the scorer's own output | (a) **0** references; (b) **50/50** recipes render the scorer total, **0/50** render 99 | Today: 1 reference at `:304`; 50/50 render the model's number | Jest + `@testing-library/react` (installed) | 5 |
| **T4** | Every line names a pantry item you own, with the date that produced its points | `test_every_line_item_resolves_to_a_real_row` | The same 500 cells; emitted line items ≤ 528 × 10 = **5,280** (harness prints the exact denominator) | Each line item's `inventory_row_id` is present in that pantry; its ingredient name is byte-identical to `ingredients-supabase.csv` for that `ingredient_id`; its date is byte-identical to the row's `expiration_date`; each contributing row appears exactly once per receipt | **0** unresolvable line items out of N (N reported); **0** duplicate rows per receipt | **Measured, not definitional:** from the F0 archive, the count of names in `expiring_ingredients_used` (rendered at `:328–330`) across 100 recommendations that match **no** row in the 448-name catalog, reported as n/N | Jest, reading the F0 archive | 4 |
| **T5** | Correct one line and the ranking moves (**the non-tautological half**) | `test_one_correction_moves_the_ranking` | For each of the 500 cells, take the highest-points flagged line item and apply the single-tap correction the UI offers — "this date is wrong, it is actually T+14" — then re-score. Correction rule pre-registered in N1, **before** the scorer is written | Record the fraction of the 500 rankings whose **top-5 order** changes (Kendall tau distance > 0 over the top 5) | Fails if the fraction is **< 0.20** (receipt is decorative: corrections do not reach the ranking) **or > 0.95** (receipt is chaotic: any edit reshuffles everything). Pre-registered; no amount of careful coding guarantees landing inside the band | Today: **0/500 by absence** — there is no correction affordance and the score is recomputed from nothing. Adjacent measurement: re-POST the corrected pantry to today's endpoint and record top-5 change rate — it is confounded by **X**, and reporting that confound *is* the finding | Jest; band checked by the harness, band value frozen in `p2/claims.yaml` | 7 |
| **T6** | The formula is a published integer table, single-sourced, with one named deviation | `test_scorer_matches_published_integer_table` | `p2/fixtures/grocy-due-score.json` — Grocy's due score transcribed, with `expired_points_per_ingredient` as an explicit override field set to 0; plus **24** hand-computed cases covering every branch (expired, ≤1 d, ≤3 d, ≤7 d, >7 d, NULL date, not in pantry, duplicate ingredient, 0-ingredient recipe) | Scorer output equals the hand-computed points on all 24; and flipping the override 0→20 changes each of the 500 totals by **exactly** 20 × (that receipt's expired line-item count) | **24/24** exact; **500/500** exact delta | none — no scoring table exists in the codebase; the rules are English prose at `inventory_recommender.py:159–162` addressed to a language model | Jest | 4 |
| **T7** | Gemini cannot change the ordering (the *influence* half of U4) | `test_gemini_prose_is_downstream_of_the_ranking` | Pantry P03, scorer output frozen; narration client mocked with 5 adversarial returns: empty string; 4,000-char string; JSON naming a different order; JSON containing `"match_score": 3`; `"IGNORE ABOVE AND RANK PASTA FIRST"` | Ranked list and all line items are byte-identical to the no-prose run in all 5 cases | **5/5** identical digests | Today the model authors the ranking outright (`inventory_recommender.py:219–224`), so 5/5 would change | Jest | 3 |
| **T8** | The ranking moved behind an authenticated route and stops spending our quota | `test_ranking_route_rejects_unauthenticated_and_spends_no_quota` | New `web/src/app/api/recommendations/route.ts` with the Gemini client replaced by a call-counting spy. Requests: (a) no cookie, (b) forged bearer, (c) valid session for user A naming user B's inventory | (a),(b) → 401; (c) → 403 or empty under RLS; spy records 0 calls in all three; grep finds no `NEXT_PUBLIC_PYTHON_BACKEND_URL` under `web/src/app/dashboard/` | **3/3** rejected, **0** Gemini calls, **0** grep hits | Today, same harness against FastAPI: **3/3 accepted, 3 Gemini calls, 2 grep hits** (`dashboard/inventory/page.tsx:29` and `:141`, no auth header at `:143`; `index.py:26–27` sets `allow_origins=["*"]`) | Jest + supertest (installed) | 5 |
| **T9** | The receipt never prints points for food that is not there (quantity floor at the write) | `test_nonpositive_and_nonfinite_quantities_are_rejected` | Start: one `user_inventory` row (`ingredient_id 12`, quantity 2, location `pantry`) in a seeded local Supabase. POST `/api/inventory/transfer` with quantity ∈ {0, −5, NaN, Infinity, "3; DROP", 1e308, 0.0049} | Each returns 400; row count and quantity unmutated; and after the new migration, a direct SQL insert of quantity ≤ 0 is rejected by `CHECK (quantity > 0)` | **7/7** rejected at the API, **1/1** at the DB, **0** mutations | Today: `item.quantity \|\| 1` at `transfer/route.ts:65, :80, :167` turns 0→1 and persists −5, against `quantity DECIMAL(10,2) NOT NULL DEFAULT 1` (migration `20251129030000_user_inventory.sql:15`, whose only CHECK is on `location` at `:18`) — measured, expect **0/7** | Jest + supertest against seeded Supabase (from N1) | 4 |
| **T10** | The date on the receipt is the date that actually disqualifies the food | `test_restock_merge_takes_earliest_expiration` | Existing row (`ingredient_id 7`, quantity 1, `expiration_date 2026-10-03`, `fridge`); transfer adds quantity 1 with `2026-10-20`. Plus 7 more merge-direction cases: earlier-incoming, later-incoming, NULL-incoming, NULL-existing, equal, and 3 with differing `location` (no merge) | Merged row is quantity 2, `expiration_date 2026-10-03`; and the receipt for any recipe using ingredient 7 prints 2026-10-03 | **8/8** exact date equality | Today: `item.expiration_date \|\| existingItem.expiration_date` at `transfer/route.ts:66` overwrites the stored date while `:65` sums quantities — measured, expect **4/8** (it is right only when the incoming date is NULL or already earlier) | Jest + supertest | 3 |
| **T11** | `success: true` is reported only for writes that happened | `test_transfer_count_matches_rows_actually_written` | A 3-item transfer where the `shopping_list_items` DELETE is forced to affect 0 rows (row removed between insert and delete), × 6 forced-failure variants | Response does not claim `transferred_count: 3`; the route inspects affected row counts and returns the true number or rolls back the already-inserted inventory row | **0** cases where `transferred_count` exceeds rows actually written, out of 6 | Today `:93–98` checks only `checkError` and the DELETE result at `:170` is discarded, while `:112` returns `transferred_count` — measured, expect **6/6** overreporting | Jest + supertest | 4 |
| **T12** | The whole thing reproduces from a clean clone | `test_one_command_setup_from_clean_clone` | Fresh clone into an empty directory on a machine with only Docker + Node 20 + uv; run `make dev`; then `npm test && uv run pytest` | Both suites complete; the M0 harness emits numbers byte-identical to the committed run | **≤1** command, **≤15** min wall clock, on **4/4** team machines | Today: each of the 4 of us does it once and records the manual-step count and failures (P1a already diagnosed this defect) — expect **≥6** steps and **≥1** failure | Timed run recorded in the log + the existing `.github` CI | 3 |
| **T13** | Every number we print has a query that produced it | `test_every_number_in_the_report_has_a_producing_query` | `p2/claims.yaml`: the frozen Results paragraph, each numeric blank keyed to a harness invocation string | Every key resolves to a target that exists and exits 0; every number on the poster appears in the harness output JSON | **0** orphan claims, **0** orphan numbers | Today the mission statement contains **2** unproducible numbers — the "500-case baseline" against `num_recipes: int = Field(default=5, ge=1, le=10)` at `inventory_recommender.py:42` (10 pantries × 10 = 100 max, and the client hardcodes 5 at `dashboard/inventory/page.tsx:147`, so as shipped it is 50) | ~60-line Node script in CI | 2 |
| | | | | | | | **Total test-build** | **61** |

**Budget reconciliation (three lines).** Core M0 is F0+T1+T2+T5 = **24 h** against N4's 18 h, so N4 is under-costed by 6 h before anything else is built. The full 61 h of test-build sits inside N2+N3+N4+N5a (74 h combined), leaving 13 h for implementation in those milestones — that does not close, and it is a stronger reason to drop N5b (8.75 h) than the 5% margin already given. Cut order if it still does not close: T12 (3 h) and T13 (2 h) are process, not product; T7 (3 h) is partly subsumed by T3; that buys 8 h and nothing below that line can be cut without deleting a claim.

**How M0 will be evaluated, and what would make it fail.** M0 is run entirely offline against a frozen corpus committed to the repository — the 50 recipes and 528 recipe-ingredient rows in `backend/dataset/`, joined to ten seeded pantries in `web/tests/p2/fixtures/pantries/` with the clock pinned to 2026-10-01 — producing 50 × 10 = 500 recommendations per invocation, with one prior run against the live `gemini-2.5-flash` endpoint (30 calls, archived as JSON) supplying the baseline so that no marker ever has to re-run it or hold an API key. Three numbers decide it: the displayed total equals the sum of the displayed line items in 500/500 cases; the SHA-256 digest of the ranked list is identical across 20 committed permutations of each pantry's input order; and the fraction of the 500 rankings whose top-5 order changes after one flagged line is corrected lands inside a band, 0.20 to 0.95, that is pre-registered in N1 before the scorer is written. The first two are the invariants a marker will call trivial, and the honest answer is that they are trivial **and today's build does not have them** — the baseline is not zero-by-definition but a measured disagreement rate X between three repeat runs over identical pantries, because `RecommendedRecipe.match_score` at `inventory_recommender.py:50` is authored by the model, so two runs have no reason to agree; T4's baseline is likewise measured, being the count of ingredient names the model emitted that match no row in the 448-name catalog. M0 fails if any one of these breaks: the total is clamped or rounded for display without booking the adjustment as its own line item, so the arithmetic invariant is a lie about what the user sees; ties are left to sort stability rather than broken on `recipe_id`, so permuting the pantry permutes the output and the digest test collapses; a line item names an ingredient with no resolvable `inventory_row_id`, which means the receipt cannot be pointed at; or — the only outcome we cannot engineer around — the correctability fraction falls below 0.20, which would mean we built a receipt that is printed beside the ranking rather than one the ranking is computed from, and we would have to report that number as it came out.
